#include <stdio.h>
#include "dog.h"
#include <string.h>
/**
 * init_dog - init a new dog
 *
 *
 */
void init_dog(struct dog *d, char *name, float age, char *owner)
{
	if (d == NULL)
	{
		p->name = name;
		p->age = age;
		p->owner = owner;
	}
}
